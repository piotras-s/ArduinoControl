{% extends 'ArduinoBundle::index.html.twig' %}

{% block content %}

    <div class="row">
        <div class="col-lg-10">
            <h1>Thermometer delete</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-5">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn-lg btn btn-primary">
                Delete this thermometer
            </button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
