{% extends '@Arduino/index.html.twig' %}

{% block content %}
    <div class="row">
        <h1>Settings <small>Connector settings</small></h1>
    </div>
    <div class="row">
        <div class="col-lg-6 well">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-large btn-primary">Submit</button>
        {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
